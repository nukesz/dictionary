version: '2'
services:
    dictionary-postgresql:
        container_name: dictionary-postgresql
        image: postgres:9.5.4
        # volumes:
        #     - ~/volumes/jhipster/dictionary/postgresql/:/var/lib/postgresql/
        environment:
            - POSTGRES_USER=dictionary
            - POSTGRES_PASSWORD=
        ports:
            - 5432:5432
